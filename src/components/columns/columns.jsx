import { addColumns, deleteColumn, editColumnTitle } from "./columns.utils";

export const Columns = () => {
  return (
    <>
      <button onClick={addColumns}>Add Columns</button>
      <h1>Hello World</h1>
    </>
  );
};
